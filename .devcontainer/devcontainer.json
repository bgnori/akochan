{
  "name": "akochan-build",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "postCreateCommand": "cd ai_src && make -f Makefile_Linux && cd .. && make -f Makefile_Linux"
}
